# 老师主动接单功能规格

## 概述

开放需求大厅供老师主动浏览并申请家长发布的需求，形成「系统自动匹配推送 + 老师主动申请」的双轨模式，增加老师的参与感和订单机会，同时让家长能从更多候选老师中做出更好的选择，提升平台整体成单率。

---

## 现有架构

当前平台仅有单向的系统匹配推送流程：家长发布需求 → 匹配算法（技能 25pt + 位置 30pt + 价格 25pt + 时间 20pt）计算分数 → 向匹配老师发送通知（`notifications` 表，`type = 'match_demand'`）→ 匹配结果记录在 `match_logs` 表。

老师只能被动接收通知，无法主动浏览需求列表，也无法针对特定需求主动申请，错失对自己有特别优势的需求。

---

## 功能详情

### 用户流程

#### 老师端：浏览并申请需求

1. 老师进入「需求大厅」（`/teacher/demand-hall`）
2. 浏览所有 `status = 'open'` 的需求列表（家长信息脱敏显示）
3. 可按**服务类别**、**城市**、**预算范围**筛选
4. 点击某需求查看详情（含服务类型、时间偏好、预算范围、孩子年龄、特殊要求）
5. 点击「申请接单」，填写：
   - 自我介绍（针对该需求的匹配理由，≤ 500 字）
   - 报价（`/小时`，需在家长预算范围内，或可超出但需说明）
6. 提交申请，进入「待家长审核」状态
7. 收到家长接受/拒绝通知
8. 被接受后，系统自动创建订单（或引导双方沟通确定订单细节）

**已申请限制**：
- 同一老师对同一需求只能申请一次
- 老师每日最多申请 10 个需求（防刷）
- 已被其他老师成功接单（`demand.status != 'open'`）的需求不可申请

#### 家长端：查看并处理申请

1. 家长在「我的需求」中，若某需求有新申请，显示「N 个老师申请」角标
2. 点击进入「申请列表」，展示所有申请该需求的老师卡片：
   - 老师姓名（全名）、头像、认证状态
   - 老师评分、历史订单数
   - 申请报价
   - 自我介绍摘要
3. 点击老师卡片查看完整申请详情
4. 操作：
   - **接受**：该申请变为 `accepted`，其余申请自动变为 `rejected`（系统通知），需求状态更新为 `matched`
   - **拒绝**：该申请变为 `rejected`，通知老师
5. 接受后，引导家长与老师进入聊天或直接创建订单

---

### 数据库设计

#### 新增表：`demand_applications`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER PK | 自增主键 |
| `demand_id` | INTEGER NOT NULL | references `demands.id` |
| `teacher_id` | INTEGER NOT NULL | references `users.id`（申请老师） |
| `introduction` | TEXT NOT NULL | 自我介绍（针对该需求） |
| `quoted_price` | INTEGER NOT NULL | 老师报价（元/小时） |
| `status` | TEXT NOT NULL DEFAULT `'pending'` | `pending` / `accepted` / `rejected` / `withdrawn` |
| `parent_note` | TEXT | 家长拒绝原因（可选） |
| `created_at` | INTEGER | 申请提交时间戳 |
| `updated_at` | INTEGER | 状态更新时间戳 |

**联合唯一约束**：`(demand_id, teacher_id)`，防止重复申请。

**查询索引建议**：
- `CREATE INDEX idx_demand_applications_demand ON demand_applications(demand_id, status);`
- `CREATE INDEX idx_demand_applications_teacher ON demand_applications(teacher_id, created_at DESC);`

---

### API 设计

| Method | Path | 描述 |
|---|---|---|
| `GET` | `/api/demand-hall` | 老师查看需求大厅（脱敏，支持筛选分页） |
| `GET` | `/api/demand-hall/:demandId` | 老师查看单条需求详情（脱敏） |
| `POST` | `/api/demand-hall/:demandId/apply` | 老师提交接单申请 |
| `DELETE` | `/api/demand-applications/:id` | 老师撤回申请（仅 `pending` 状态可撤回） |
| `GET` | `/api/demands/:demandId/applications` | 家长查看某需求的所有申请列表 |
| `POST` | `/api/demand-applications/:id/accept` | 家长接受申请 |
| `POST` | `/api/demand-applications/:id/reject` | 家长拒绝申请（附原因） |
| `GET` | `/api/teacher/my-applications` | 老师查看自己的申请历史 |

**需求大厅脱敏规则：**

```typescript
// 家长信息脱敏：不暴露家长 ID、姓名、电话
{
  id: demand.id,
  childAge: demand.childAge,
  childGender: demand.childGender,
  serviceCategory: demand.serviceCategory,
  specificService: demand.specificService,
  serviceType: demand.serviceType,
  location: demand.city + demand.district, // 不暴露详细地址
  preferredTime: demand.preferredTime,
  budgetMin: demand.budgetMin,
  budgetMax: demand.budgetMax,
  specialRequirements: demand.specialRequirements,
  // ❌ 不包含：parentId, parentName, parentPhone
  applicationCount: count,   // 已有多少老师申请
  myApplicationStatus: '...' // 当前老师的申请状态（若有）
}
```

**提交申请请求体：**

```typescript
{
  introduction: string,   // 1–500 字
  quotedPrice: number     // 元/小时，正整数
}
```

---

### 前端页面

| 页面/组件 | 类型 | 说明 |
|---|---|---|
| `DemandHallPage` | 新增页面 | 老师端需求大厅（`/teacher/demand-hall`） |
| `DemandHallCard` | 新增组件 | 需求卡片，展示关键信息和「申请」按钮 |
| `DemandHallFilter` | 新增组件 | 筛选栏（服务类别 / 城市 / 预算范围） |
| `ApplyModal` | 新增组件 | 申请弹窗（自我介绍 + 报价输入） |
| `MyApplicationsPage` | 新增页面 | 老师端「我的申请」列表（`/teacher/my-applications`） |
| `DemandApplicationListPage` | 新增页面 | 家长端查看某需求的申请列表（`/demands/:id/applications`） |
| `ApplicationTeacherCard` | 新增组件 | 申请列表中的老师卡片，含接受/拒绝操作 |
| 需求列表页（家长端） | 修改 | 在每个需求上显示「N 个申请」角标 |
| 通知详情 | 修改 | 支持「申请被接受/拒绝」类型的通知内容渲染 |

---

## 技术方案

### 与现有匹配推送的关系

两套机制互补，不冲突：

| 维度 | 系统自动推送 | 老师主动申请 |
|---|---|---|
| **触发时机** | 家长发布需求后即时触发 | 老师随时浏览大厅后主动触发 |
| **筛选标准** | 匹配算法（评分 Top N） | 老师自主判断是否适合 |
| **家长体验** | 被动等待推荐老师联系 | 主动浏览候选老师申请 |
| **老师体验** | 被动收到通知 | 主动发现并申请感兴趣的需求 |
| **数据表** | `match_logs` + `notifications` | `demand_applications` |
| **互动**  | 老师看到通知后可进入详情页 | 老师提交申请，家长审核 |

**协同机制**：系统推送的老师和主动申请的老师都会出现在家长的申请列表中，由家长统一审核。

### 申请状态流转

```
老师提交申请
    ↓
pending（待家长审核）
    ├→ accepted（家长接受）→ 触发 demand.status = 'matched'，其余申请自动 rejected
    ├→ rejected（家长拒绝）→ 通知老师
    └→ withdrawn（老师撤回，仅 pending 状态可操作）
```

### 每日限额实现

```typescript
// 使用当日零点到现在的申请数做判断
const today = new Date();
today.setHours(0, 0, 0, 0);

const todayCount = await db.select({ count: sql`count(*)` })
  .from(demandApplications)
  .where(and(
    eq(demandApplications.teacherId, teacherId),
    gte(demandApplications.createdAt, today)
  ));

if (todayCount[0].count >= 10) {
  throw new Error('每日申请上限为 10 个');
}
```

---

## 安全考虑

- **家长信息保护**：需求大厅 API 绝不返回 `parentId`、家长姓名、手机号等身份信息，只在家长接受申请后，才通过聊天系统开放沟通渠道
- **权限验证**：只有 `role = 'teacher'` 的用户才能访问需求大厅和提交申请；只有需求的 `parentId` 对应用户才能查看申请列表和操作
- **防刷申请**：每日限额 10 个 + 同需求唯一约束，防止老师批量刷申请骚扰家长
- **幂等性**：接受申请的 API 需在事务中执行（更新申请状态 + 更新需求状态 + 拒绝其他申请），防止并发接受多个申请

---

## 开发工作量预估

| 任务 | 预估工时 |
|---|---|
| 数据库 schema 变更 + migration + 索引 | 2h |
| 需求大厅 API（脱敏列表 + 筛选 + 单条详情） | 5h |
| 提交申请 API（含每日限额校验） | 3h |
| 家长查看申请列表 API | 3h |
| 接受/拒绝申请 API（含事务处理） | 4h |
| 老师撤回申请 API | 1h |
| 申请相关通知触发 | 3h |
| 老师端需求大厅页面 + 筛选 + 申请弹窗 | 10h |
| 老师端「我的申请」列表页 | 4h |
| 家长端申请列表页 + 操作 | 6h |
| 家长端需求列表角标 | 2h |
| 测试（并发接受、权限、脱敏） | 4h |
| **合计** | **47h（约 6 个工作日）** |

---

## 优先级与依赖

- **优先级：P1** — 显著提升成单率，对老师侧体验改善明显
- **前置依赖**：无强依赖，可独立开发
- **建议顺序**：在文档 `04-聊天系统.md` 之后或同步开发，家长接受申请后可自然引导进入聊天
- **后续影响**：
  - 申请成功率（申请数 / 成单数）可作为文档 `06-数据看板.md` 的关键漏斗指标
  - 文档 `03-微信小程序.md` Phase 2 需实现小程序端需求大厅页面
